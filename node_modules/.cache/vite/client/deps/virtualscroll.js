import {
  __commonJS
} from "./chunk-JVWSFFO4.js";

// node_modules/virtualscroll/index.js
var require_virtualscroll = __commonJS({
  "node_modules/virtualscroll/index.js"(exports, module) {
    (function(definition) {
      "use strict";
      if (typeof exports === "object" && typeof module === "object") {
        module.exports = definition;
      } else if (typeof define === "function" && define.amd) {
        define(definition);
      } else if (typeof window !== "undefined" || typeof self !== "undefined") {
        let global = typeof window !== "undefined" ? window : self;
        global.VirtualScroll = definition;
      } else {
        throw new Error("This environment was not anticipated.");
      }
    })(function(RootElement, UserConfig) {
      if (!RootElement || RootElement.nodeName !== "DIV") {
        throw new Error("(VirtualScroll) The first parameter must be a DIV element");
      }
      if (UserConfig && UserConfig.constructor.valueOf() !== Object) {
        throw new Error("(VirtualScroll) The second argument must be an Object!");
      } else if (UserConfig && UserConfig.beforeCreate) {
        UserConfig.beforeCreate.call(RootElement);
      }
      ;
      const DefaultConfig = {
        CurrentPosition: [0, 0],
        // 滑块的当前位置。默认：[0,0], 第1个元素表示水平滚动滑块滑动距离的百分比, 第2个元素表示垂直滚动滑块滑动距离的百分比, 取值 0 - 1，表示百分比
        VBwidth: 16,
        // 垂直滚动条的宽度 默认 16px
        HBheight: 16,
        // 水平滚动条的高度 默认 16px
        VSwidth: 16,
        // 垂直滑块的宽度 默认 16px
        HSheight: 16,
        // 水平滑块的高度 默认 16px
        VBborder: "none",
        // 垂直滚动条的边线 例如： "1px solid #000" 或 "1px solid rgba(255,0,0,.5)"
        HBborder: "none",
        // 水平滚动条的边线 例如： "1px solid #000" 或 "1px solid rgba(255,0,0,.5)"
        VSborder: "none",
        // 垂直滑块的边线 例如： "1px solid #000" 或 "1px solid rgba(255,0,0,.5)"
        HSborder: "none",
        // 水平滑块的边线 例如： "1px solid #000" 或 "1px solid rgba(255,0,0,.5)"
        VBbgColor: "#f0f0f0",
        // 垂直滚动条的背景颜色(透明度请用RGBA)
        HBbgColor: "#f0f0f0",
        // 水平滚动条的背景颜色(透明度请用RGBA)
        VSbgColor: "#cdcdcd",
        // 垂直滚动滑块的背景颜色(透明度请用RGBA)
        VSoverColor: "#ababab",
        // 垂直滚动滑块鼠标经过的颜色(透明度请用RGBA)
        VSclickColor: "#606060",
        // 垂直滚动滑块鼠标点击的颜色(透明度请用RGBA)
        HSbgColor: "#cdcdcd",
        // 水平滚动滑块的背景颜色(透明度请用RGBA)
        HSoverColor: "#ababab",
        // 水平滚动滑块鼠标经过的颜色(透明度请用RGBA)
        HSclickColor: "#606060",
        // 水平滚动滑块鼠标点击的颜色(透明度请用RGBA)
        VBradius: "0",
        // 垂直滚动条圆角 可以输入一个字符串 '4px' 或 '2em 1em 4em / 0.5em 3em' 或 '4px 4px'
        VSradius: "0",
        // 垂直滚动滑块圆角 可以输入一个字符串 '4px' 或 '2em 1em 4em / 0.5em 3em' 或 '4px 4px'
        HBradius: "0",
        // 水平滚动条圆角 可以输入一个字符串 '4px' 或 '2em 1em 4em / 0.5em 3em' 或 '4px 4px'
        HSradius: "0",
        // 水平滚动滑块圆角 可以输入一个字符串 '4px' 或 '2em 1em 4em / 0.5em 3em' 或 '4px 4px'
        VScursor: "default",
        // 鼠标移动到垂直滚动滑块上的形状，默认是 default
        HScursor: "default",
        // 鼠标移动到水平滚动滑块上的形状，默认是 default
        WheelNeed: true,
        // 是否需要滚轮滚动，默认是 true , 如果不需要，设置为 false
        WheelStep: 80,
        // 滚轮滚动时单步滑动的距离 单位是 px (也包括鼠标点击滚动条时的单步滑动距离)
        WheelBubble: true,
        // 当滚轮滚动到底或顶时，是否将事件冒泡到 document , 设置 false 表示不冒泡到 document , true 表示冒泡到 document
        AutoShow: false,
        // 鼠标移入时显示移出时隐藏滚动条，true 表示响应鼠标移入移出， false 表示不响应鼠标移入移出
        TransitionDuration: ".2s",
        // 动画的执行时间(影响滚动条的颜色切换时间，影响滑块的透明度切换时间)
        beforeCreate() {
        },
        // 钩子 渲染之前 ，函数内 this 指向 RootElement
        created() {
        },
        // 钩子 渲染之后 无参数, 函数内 this 指向 VirtualScroll 实例
        wheels(orientation, position) {
        },
        // 钩子 滚轮滚动时执行  orientation 表示滚动方向 'up' 向上滚动 'down' 向下滚动 ,参数 position 是表示垂直滚动条的滑动偏移量 CurrentPosition
        dragStart(movetype, position) {
        },
        // 钩子 拖动开始  movetype 表示滚动条类型 H 表示水平，V表示垂直 参数 position 是表示垂直滚动条的滑动偏移量 CurrentPosition
        dragging(orientation, position) {
        },
        // 钩子 拖动进行中 参数1是移动方向 orientation (上'up', 下'down', 左'left', 右'right' ) ,参数 position 是表示垂直滚动条的滑动偏移量 CurrentPosition
        dragEnd(movetype, position) {
        }
        // 钩子 拖动结束  movetype 表示滚动条类型 H 表示水平，V表示垂直 参数 position 是表示垂直滚动条的滑动偏移量 CurrentPosition
      };
      const Template = `<div class="vs-main"><div class="vs-container"></div></div><div class="vs-horizontal-bar"><div class="vs-horizontal-slider"></div></div><div class="vs-vertical-bar"><div class="vs-vertical-slider"></div></div>`;
      function VirtualScroll(RootElement2, UserConfig2) {
        this.$Options = {};
        this.$Element = RootElement2;
        this.$Main = null;
        this.$Container = null;
        this.$HorizontalBar = null;
        this.$HorizontalSlider = null;
        this.$VerticalBar = null;
        this.$VerticalSlider = null;
        this._build();
      }
      VirtualScroll.prototype = {
        constructor: VirtualScroll,
        // 重新指向构造函数
        /**
         * 成员函数执行汇总
         */
        _build() {
          this._init();
          this._setStyle();
          this._verticalAction();
          this._horizontalAction();
          this._containerWheel();
          this._initEvent();
          this.$Options.created.call(this);
        },
        /**
         * DOM结构与成员变量初始化
         */
        _init() {
          this.$Options = Object.assign({}, DefaultConfig, UserConfig);
          let contents = this.$Element.innerHTML;
          this.$Element.innerHTML = Template;
          this.$Main = this.$Element.querySelector(".vs-main");
          this.$Container = this.$Main.querySelector(".vs-container");
          this.$Container.innerHTML = contents;
          this.$HorizontalBar = this.$Element.querySelector(".vs-horizontal-bar");
          this.$HorizontalSlider = this.$Element.querySelector(".vs-horizontal-slider");
          this.$VerticalBar = this.$Element.querySelector(".vs-vertical-bar");
          this.$VerticalSlider = this.$Element.querySelector(".vs-vertical-slider");
          if (Array.isArray(this.$Options.CurrentPosition)) {
            let ocp = this.$Options.CurrentPosition;
            if (ocp[0] > 100) {
              ocp[0] = 100;
            } else if (ocp[0] < 0 || isNaN(ocp[0])) {
              ocp[0] = 0;
            }
            if (ocp[1] > 100) {
              ocp[1] = 100;
            } else if (ocp[1] < 0 || isNaN(ocp[1])) {
              ocp[1] = 0;
            }
          } else {
            this.$Options.CurrentPosition = [0, 0];
          }
        },
        /**
         * 给元素批量加样式（工具函数）
         */
        _addCSS(element, Obj) {
          for (let key in Obj) {
            element.style[key] = Obj[key];
          }
        },
        /**
         * 初始化样式
         */
        _setStyle() {
          let op = this.$Options;
          let el = this.$Element;
          let ma = this.$Main;
          let co = this.$Container;
          let vb = this.$VerticalBar;
          let vs = this.$VerticalSlider;
          let hb = this.$HorizontalBar;
          let hs = this.$HorizontalSlider;
          if (op.AutoShow) {
            hb.style.opacity = 0;
            vb.style.opacity = 0;
          }
          ;
          el.style.overflow = "hidden";
          let ElementStyle = window.getComputedStyle(this.$Element, null);
          if (!ElementStyle.position || ElementStyle.position === "static") {
            el.style.position = "relative";
          }
          this._addCSS(ma, { position: "absolute", top: 0, left: 0, margin: 0, padding: 0, border: "none", background: "none" });
          let ContainerStyle = window.getComputedStyle(this.$Container, null);
          if (ContainerStyle.position !== "absolute") {
            co.style.position = "absolute";
          }
          this._addCSS(co, { top: 0, left: 0, margin: 0, padding: 0, border: "none", background: "none" });
          this._addCSS(vb, {
            position: "absolute",
            top: 0,
            right: 0,
            width: op.VBwidth + "px",
            backgroundColor: op.VBbgColor,
            borderRadius: op.VBradius,
            boxSizing: "border-box",
            padding: 0,
            margin: 0,
            border: op.VBborder,
            transitionProperty: "opacity",
            transitionDuration: op.TransitionDuration
          });
          this._addCSS(vs, {
            position: "absolute",
            boxSizing: "border-box",
            padding: 0,
            margin: 0,
            border: op.VSborder,
            backgroundColor: op.VSbgColor,
            borderRadius: op.VSradius,
            cursor: op.VScursor,
            left: "50%",
            transitionProperty: "background",
            transitionDuration: op.TransitionDuration,
            width: (op.VSwidth < vb.clientWidth ? op.VSwidth : vb.clientWidth) + "px",
            marginLeft: (op.VSwidth < vb.clientWidth ? op.VSwidth : vb.clientWidth) / -2 + "px"
          });
          this._addCSS(hb, {
            position: "absolute",
            bottom: 0,
            left: 0,
            height: op.HBheight + "px",
            backgroundColor: op.HBbgColor,
            borderRadius: op.HBradius,
            boxSizing: "border-box",
            padding: 0,
            margin: 0,
            border: op.HBborder,
            transitionProperty: "opacity",
            transitionDuration: op.TransitionDuration
          });
          this._addCSS(hs, {
            position: "absolute",
            boxSizing: "border-box",
            padding: 0,
            margin: 0,
            border: op.HSborder,
            backgroundColor: op.HSbgColor,
            borderRadius: op.HSradius,
            cursor: op.HScursor,
            top: "50%",
            transitionProperty: "background",
            transitionDuration: op.TransitionDuration,
            height: (op.HSheight < hb.clientHeight ? op.HSheight : hb.clientHeight) + "px",
            marginTop: (op.HSheight < hb.clientHeight ? op.HSheight : hb.clientHeight) / -2 + "px"
          });
          if (co.offsetHeight <= el.clientHeight) {
            vb.style.display = "none";
            ma.style.right = 0;
            op.CurrentPosition[1] = 0;
          } else {
            vb.style.display = "block";
            ma.style.right = op.VBwidth + "px";
          }
          if (co.offsetWidth <= el.clientWidth) {
            hb.style.display = "none";
            ma.style.bottom = 0;
            op.CurrentPosition[0] = 0;
          } else {
            hb.style.display = "block";
            ma.style.bottom = op.HBheight + "px";
          }
          vb.style.height = el.clientHeight - hb.offsetHeight + "px";
          vs.style.height = vb.clientHeight / co.offsetHeight * el.clientHeight + "px";
          vs.style.top = op.CurrentPosition[1] * (vb.clientHeight - vs.offsetHeight) + "px";
          hb.style.width = el.clientWidth - vb.offsetWidth + "px";
          hs.style.width = hb.clientWidth / co.offsetWidth * el.clientWidth + "px";
          hs.style.left = op.CurrentPosition[0] * (hb.clientWidth - hs.offsetWidth) + "px";
          co.style.left = -(co.offsetWidth - ma.clientWidth) * op.CurrentPosition[0] + "px";
          co.style.top = -(co.offsetHeight - ma.clientHeight) * op.CurrentPosition[1] + "px";
        },
        /**
         * 文档结构还原
         */
        restore() {
          let co = this.$Container;
          let el = this.$Element;
          el.innerHTML = co.innerHTML;
        },
        /**
         * 当窗口大小发生变化或其它原因，需要重新渲染滚动条时触发
         * 本实例自带window.onresize响应
         * 如果是其它事件导致的窗口大小变化，那可以在那个事件中 执行 vsobj.refresh() 刷新一下滚动条位置及长度
         */
        refresh() {
          let op = this.$Options;
          let el = this.$Element;
          let ma = this.$Main;
          let co = this.$Container;
          let vb = this.$VerticalBar;
          let vs = this.$VerticalSlider;
          let hb = this.$HorizontalBar;
          let hs = this.$HorizontalSlider;
          let HShow = true;
          let VShow = true;
          if (co.offsetWidth <= el.clientWidth) {
            HShow = false;
            hb.style.display = "none";
            op.CurrentPosition[0] = 0;
            co.style.left = 0;
            ma.style.bottom = 0;
          }
          if (co.offsetHeight <= el.clientHeight) {
            VShow = false;
            vb.style.display = "none";
            op.CurrentPosition[1] = 0;
            co.style.top = 0;
            ma.style.right = 0;
          }
          if (HShow) {
            hb.style.display = "block";
            ma.style.bottom = op.HBheight + "px";
            hb.style.width = el.clientWidth - (VShow ? op.VBwidth : 0) + "px";
            hs.style.width = hb.clientWidth / co.offsetWidth * el.clientWidth + "px";
            hs.style.left = op.CurrentPosition[0] * (hb.clientWidth - hs.offsetWidth) + "px";
            co.style.left = -(co.offsetWidth - ma.clientWidth) * op.CurrentPosition[0] + "px";
          }
          if (VShow) {
            vb.style.display = "block";
            ma.style.right = op.VBwidth + "px";
            vb.style.height = el.clientHeight - (HShow ? op.HBheight : 0) + "px";
            vs.style.height = vb.clientHeight / co.offsetHeight * el.clientHeight + "px";
            vs.style.top = op.CurrentPosition[1] * (vb.clientHeight - vs.offsetHeight) + "px";
            co.style.top = -op.CurrentPosition[1] * (co.offsetHeight - ma.clientHeight) + "px";
          }
        },
        /**
         * 阻止浏览器默认事件，和事件冒泡
         */
        _stopBehavior(event) {
          if (event.preventDefault) {
            event.preventDefault();
            event.stopPropagation();
          } else {
            event.cancelBubble = true;
            event.returnValue = false;
          }
        },
        /**
         * 渲染水平滚动条元素
         */
        _horizontalAction() {
          let self2 = this;
          let hb = this.$HorizontalBar;
          let hs = this.$HorizontalSlider;
          let op = this.$Options;
          let co = this.$Container;
          let oldLeft = 0;
          let orientation = null;
          let disX = null;
          let docup = function(event) {
            op.dragEnd.call(self2, "H", op.CurrentPosition[0]);
            document.removeEventListener("mousemove", docmove);
            document.removeEventListener("mouseup", docup);
          };
          let docmove = function(event) {
            self2._stopBehavior(event);
            let HoffsetRange = hb.clientWidth - hs.offsetWidth;
            let HcoverWidth = co.offsetWidth - self2.$Main.clientWidth;
            let newLeft = event.clientX - disX;
            if (newLeft < 0) {
              newLeft = 0;
            }
            ;
            if (newLeft > HoffsetRange) {
              newLeft = HoffsetRange;
            }
            ;
            hs.style.left = newLeft + "px";
            co.style.left = -(newLeft * HcoverWidth / HoffsetRange) + "px";
            op.CurrentPosition[0] = newLeft / HoffsetRange;
            if (oldLeft !== newLeft) {
              if (oldLeft > newLeft) {
                orientation = "left";
              }
              if (oldLeft < newLeft) {
                orientation = "right";
              }
              self2.$Options.dragging.call(self2, orientation, op.CurrentPosition[0]);
            }
            oldLeft = newLeft;
          };
          let hsdown = function(event) {
            orientation = null;
            op.dragStart.call(self2, "H", op.CurrentPosition[0]);
            self2._stopBehavior(event);
            disX = event.clientX - hs.offsetLeft;
            document.addEventListener("mousemove", docmove);
            document.addEventListener("mouseup", docup);
          };
          hs.addEventListener("mousedown", hsdown);
        },
        /**
         * 渲染垂直滚动条元素
         */
        _verticalAction() {
          let self2 = this;
          let vb = this.$VerticalBar;
          let vs = this.$VerticalSlider;
          let op = this.$Options;
          let co = this.$Container;
          let oldTop = 0;
          let orientation = null;
          let disY = null;
          let docup = function(event) {
            op.dragEnd.call(self2, "V", op.CurrentPosition[1]);
            document.removeEventListener("mousemove", docmove);
            document.removeEventListener("mouseup", docup);
          };
          let docmove = function(event) {
            self2._stopBehavior(event);
            let VoffsetRange = vb.clientHeight - vs.offsetHeight;
            let VcoverHehght = co.offsetHeight - self2.$Main.clientHeight;
            let newTop = event.clientY - disY;
            if (newTop < 0) {
              newTop = 0;
            }
            ;
            if (newTop > VoffsetRange) {
              newTop = VoffsetRange;
            }
            ;
            vs.style.top = newTop + "px";
            co.style.top = -(newTop / VoffsetRange * VcoverHehght) + "px";
            op.CurrentPosition[1] = newTop / VoffsetRange;
            if (oldTop !== newTop) {
              if (oldTop > newTop) {
                orientation = "up";
              }
              if (oldTop < newTop) {
                orientation = "down";
              }
              self2.$Options.dragging.call(self2, orientation, op.CurrentPosition[1]);
            }
            oldTop = newTop;
          };
          let vsdown = function(event) {
            orientation = null;
            op.dragStart.call(self2, "V", op.CurrentPosition[1]);
            self2._stopBehavior(event);
            disY = event.clientY - vs.offsetTop;
            document.addEventListener("mousemove", docmove);
            document.addEventListener("mouseup", docup);
          };
          vs.addEventListener("mousedown", vsdown);
        },
        _wheelUp(event) {
          let co = this.$Container;
          let VoffsetRange = this.$VerticalBar.clientHeight - this.$VerticalSlider.offsetHeight;
          let top = parseFloat(co.style.top);
          if (top + this.$Options.WheelStep >= 0) {
            top = -0;
          } else {
            this._stopBehavior(event);
            top = top + this.$Options.WheelStep;
          }
          co.style.top = top + "px";
          let x = this.$VerticalBar.clientHeight * -top / this.$Container.offsetHeight;
          this.$VerticalSlider.style.top = x + "px";
          let ocp = x / VoffsetRange;
          this.$Options.CurrentPosition[1] = ocp;
          this.$Options.wheels.call(this, "up", ocp);
        },
        _wheelDown(event) {
          let co = this.$Container;
          let top = parseFloat(co.style.top);
          let WheelRange = this.$Container.offsetHeight - this.$Main.clientHeight;
          let VoffsetRange = this.$VerticalBar.clientHeight - this.$VerticalSlider.offsetHeight;
          if (top - this.$Options.WheelStep <= -WheelRange) {
            top = -WheelRange;
          } else {
            this._stopBehavior(event);
            top = top - this.$Options.WheelStep;
          }
          co.style.top = top + "px";
          let x = Math.abs(top * VoffsetRange / WheelRange);
          this.$VerticalSlider.style.top = x + "px";
          let ocp = x / VoffsetRange;
          this.$Options.CurrentPosition[1] = ocp;
          this.$Options.wheels.call(this, "down", ocp);
        },
        /**
         * 鼠标滚轮在滚动区滚动时，可滚动内容进行滚动
         */
        _containerWheel() {
          let self2 = this;
          let op = this.$Options;
          if (!op.WheelNeed) {
            return;
          }
          let handler = function(event) {
            if (!self2.$Options.WheelBubble) {
              self2._stopBehavior(event);
            }
            let VoffsetTop = self2.$VerticalSlider.offsetTop;
            let VoffsetRange = self2.$VerticalBar.clientHeight - self2.$VerticalSlider.offsetHeight;
            if ((event.detail < 0 || event.wheelDelta > 0) && VoffsetTop !== 0) {
              self2._wheelUp(event);
            }
            if ((event.detail > 0 || event.wheelDelta < 0) && VoffsetTop !== VoffsetRange) {
              self2._wheelDown(event);
            }
          };
          this.$Main.addEventListener("DOMMouseScroll", handler);
          this.$Main.addEventListener("mousewheel", handler);
        },
        /**
         * 滑块颜色鼠标经过和点击变色事件
         */
        _sliderColorEvent(slider, bgcolor, overcolor, clickcolor) {
          let t = true;
          let senter = function(e) {
            if (t) {
              slider.style.background = overcolor;
            }
          };
          let sout = function(e) {
            if (t) {
              slider.style.background = bgcolor;
            }
          };
          let sdown = function(e) {
            t = false;
            slider.style.background = clickcolor;
            document.addEventListener("mouseup", docup);
          };
          let sup = function(e) {
            t = true;
            slider.style.background = overcolor;
            document.removeEventListener("mouseup", docup);
          };
          let docup = function(e) {
            t = true;
            slider.style.background = bgcolor;
          };
          slider.addEventListener("mouseout", sout);
          slider.addEventListener("mouseenter", senter);
          slider.addEventListener("mousedown", sdown);
          slider.addEventListener("mouseup", sup);
        },
        /**
         * 水平单步滑动公用方法
         * n 表示单步滑动距离，如果小于 -1  或 大于 1 则是像素，如果大于-1小于1，则是百分比
         * 如果是百分比，那么输入 -1 到 1 的数值，比如  -0.25  或 0.958 （正数表示向左滑动，负数表示向右滑动）
         * @param n {Number} 是一个整数或小数，整数表示像素，小数表示百分比
         */
        horizontalSingle(n) {
          if (isNaN(n)) {
            return false;
          }
          let op = this.$Options;
          let hb = this.$HorizontalBar;
          let hs = this.$HorizontalSlider;
          let co = this.$Container;
          let ma = this.$Main;
          let HoffsetRange = hb.clientWidth - hs.offsetWidth;
          let CoffsetRange = co.offsetWidth - ma.clientWidth;
          let InitWidth = 0;
          let l = 0;
          op.dragStart.call(this, "H", op.CurrentPosition[0]);
          if (n >= -1 && n <= 1) {
            InitWidth = n * CoffsetRange;
          } else {
            InitWidth = n;
          }
          let x = InitWidth / CoffsetRange * HoffsetRange;
          if (x === 0) {
            l = HoffsetRange / 2;
          }
          if (x > 0) {
            x = Math.ceil(x);
            l = hs.offsetLeft + x > HoffsetRange ? HoffsetRange : hs.offsetLeft + x;
          }
          if (x < 0) {
            x = Math.floor(x);
            l = hs.offsetLeft + x < 0 ? 0 : hs.offsetLeft + x;
          }
          op.CurrentPosition[0] = l / HoffsetRange;
          op.dragEnd.call(this, "H", op.CurrentPosition[0]);
          hs.style.left = l + "px";
          co.style.left = -l * CoffsetRange / HoffsetRange + "px";
        },
        /**
         * 垂直单步滑动公用方法
         * n 表示单步滑动距离，如果小于 -1  或 大于 1则是像素，如果大于-1小于1，则是百分比
         * 如果是百分比，那么输入 -1 到 1 的数值，比如  -0.25  或 0.958 （正数表示向下滑动，负数表示向上滑动）
         * @param n {Number} 是一个整数或小数，整数表示像素，小数表示百分比
         */
        verticalSingle(n) {
          if (isNaN(n)) {
            return false;
          }
          let op = this.$Options;
          let vb = this.$VerticalBar;
          let vs = this.$VerticalSlider;
          let co = this.$Container;
          let ma = this.$Main;
          let VoffsetRange = vb.clientHeight - vs.offsetHeight;
          let CoffsetRange = co.offsetHeight - ma.clientHeight;
          let InitHeight = 0;
          let t = 0;
          op.dragStart.call(this, "V", op.CurrentPosition[1]);
          if (n >= -1 && n <= 1) {
            InitHeight = n * CoffsetRange;
          } else {
            InitHeight = n;
          }
          let x = InitHeight / CoffsetRange * VoffsetRange;
          if (x === 0) {
            t = VoffsetRange / 2;
          }
          if (x > 0) {
            x = Math.ceil(x);
            t = vs.offsetTop + x > VoffsetRange ? VoffsetRange : vs.offsetTop + x;
          }
          if (x < 0) {
            x = Math.floor(x);
            t = vs.offsetTop + x < 0 ? 0 : vs.offsetTop + x;
          }
          op.CurrentPosition[1] = t / VoffsetRange;
          op.dragEnd.call(this, "V", op.CurrentPosition[1]);
          vs.style.top = t + "px";
          co.style.top = -t * CoffsetRange / VoffsetRange + "px";
        },
        /**
         * 水平滚动条点击滑动
         */
        _horizontalSlide() {
          let self2 = this;
          let op = this.$Options;
          let hb = this.$HorizontalBar;
          let hs = this.$HorizontalSlider;
          let ma = this.$Main;
          let co = this.$Container;
          let timer = null;
          let orientation = null;
          let HBdown = function(event) {
            op.dragStart.call(self2, "H", op.CurrentPosition[0]);
            self2._stopBehavior(event);
            timer = setInterval(function() {
              hb.addEventListener("mouseout", hbclear);
              hb.addEventListener("mouseup", hbclear);
              let DownPoint = event.offsetX;
              if (navigator.userAgent.indexOf("Firefox") >= 0) {
                DownPoint = event.layerX;
              }
              let HoffsetRange = hb.clientWidth - hs.offsetWidth;
              let CoffsetRange = co.offsetWidth - ma.clientWidth;
              let x = HoffsetRange * op.WheelStep / CoffsetRange;
              if (DownPoint <= hs.offsetLeft + hs.offsetWidth && DownPoint >= hs.offsetLeft) {
              } else {
                let l = 0;
                if (DownPoint < hs.offsetLeft) {
                  orientation = "left";
                  l = hs.offsetLeft <= x ? 0 : hs.offsetLeft - x;
                  if (l < DownPoint) {
                    l = DownPoint;
                    clearInterval(timer);
                  }
                }
                if (DownPoint > hs.offsetLeft + hs.offsetWidth) {
                  orientation = "right";
                  l = hs.offsetLeft + x >= HoffsetRange ? HoffsetRange : hs.offsetLeft + x;
                  if (l + hs.offsetWidth > DownPoint) {
                    l = DownPoint - hs.offsetWidth;
                    clearInterval(timer);
                  }
                }
                op.CurrentPosition[0] = l / HoffsetRange;
                op.dragging.call(self2, orientation, op.CurrentPosition[0]);
                hs.style.left = l + "px";
                co.style.left = -l * CoffsetRange / HoffsetRange + "px";
              }
            }, 30);
          };
          let hbclear = function(event) {
            clearInterval(timer);
            op.dragEnd.call(self2, "H", op.CurrentPosition[0]);
            hb.removeEventListener("mouseout", hbclear);
            hb.removeEventListener("mouseup", hbclear);
          };
          hb.addEventListener("mousedown", HBdown);
        },
        /**
         * 垂直滚动条点击滑动
         */
        _verticalSlide() {
          let self2 = this;
          let op = this.$Options;
          let vb = this.$VerticalBar;
          let vs = this.$VerticalSlider;
          let ma = this.$Main;
          let co = this.$Container;
          let timer = null;
          let orientation = null;
          let VBdown = function(event) {
            op.dragStart.call(self2, "V", op.CurrentPosition[1]);
            self2._stopBehavior(event);
            timer = setInterval(function() {
              vb.addEventListener("mouseout", vbclear);
              vb.addEventListener("mouseup", vbclear);
              let DownPoint = event.offsetY;
              if (navigator.userAgent.indexOf("Firefox") >= 0) {
                DownPoint = event.layerY;
              }
              if (window.ActiveXObject || "ActiveXObject" in window) {
                DownPoint = event.offsetY;
              }
              let VoffsetRange = vb.clientHeight - vs.offsetHeight;
              let CoffsetRange = co.offsetHeight - ma.clientHeight;
              let x = VoffsetRange * op.WheelStep / CoffsetRange;
              if (DownPoint <= vs.offsetTop + vs.offsetHeight && DownPoint >= vs.offsetTop) {
              } else {
                let t = 0;
                if (DownPoint < vs.offsetTop) {
                  orientation = "up";
                  t = vs.offsetTop <= x ? 0 : vs.offsetTop - x;
                  if (t < DownPoint) {
                    t = DownPoint;
                    clearInterval(timer);
                  }
                }
                if (DownPoint > vs.offsetTop + vs.offsetHeight) {
                  orientation = "down";
                  t = vs.offsetTop + x >= VoffsetRange ? VoffsetRange : vs.offsetTop + x;
                  if (t + vs.offsetHeight > DownPoint) {
                    t = DownPoint - vs.offsetHeight;
                    clearInterval(timer);
                  }
                }
                op.CurrentPosition[1] = t / VoffsetRange;
                op.dragging.call(self2, orientation, op.CurrentPosition[1]);
                vs.style.top = t + "px";
                co.style.top = -t * CoffsetRange / VoffsetRange + "px";
              }
            }, 30);
          };
          let vbclear = function(event) {
            clearInterval(timer);
            op.dragEnd.call(self2, "V", op.CurrentPosition[1]);
            vb.removeEventListener("mouseout", vbclear);
            vb.removeEventListener("mouseup", vbclear);
          };
          vb.addEventListener("mousedown", VBdown);
        },
        /**
         * 直达某个元素所在点，一般被用作搜索书签
         * @param oNode {DOM Object} Node 滚动内容中的节点对象
         * @param h {Bloomean} 是否需要水平滑动
         * @param V {Bloomean} 是否需要垂直滑动
         */
        reachTo(oNode, h, v) {
          let op = this.$Options;
          let co = this.$Container;
          let ma = this.$Main;
          let NodeGbcr = oNode.getBoundingClientRect();
          let CoGbcr = co.getBoundingClientRect();
          if (v) {
            op.dragStart.call(this, "V", op.CurrentPosition[1]);
            let vb = this.$VerticalBar;
            let vs = this.$VerticalSlider;
            let vrange = vb.clientHeight - vs.offsetHeight;
            let crangev = co.offsetHeight - ma.clientHeight;
            let t = Math.abs(NodeGbcr.top - CoGbcr.top);
            if (t >= crangev) {
              t = crangev;
            }
            co.style.top = -t + "px";
            vs.style.top = t / crangev * vrange + "px";
            op.CurrentPosition[1] = t / crangev * vrange / vrange;
            op.dragEnd.call(this, "V", op.CurrentPosition[1]);
          }
          if (h) {
            op.dragStart.call(this, "H", op.CurrentPosition[0]);
            let hb = this.$HorizontalBar;
            let hs = this.$HorizontalSlider;
            let hrange = hb.clientWidth - hs.offsetWidth;
            let crangeh = co.offsetWidth - ma.clientWidth;
            let l = Math.abs(NodeGbcr.left - CoGbcr.left);
            if (l >= crangeh) {
              l = crangeh;
            }
            co.style.left = -l + "px";
            hs.style.left = l / crangeh * hrange + "px";
            op.CurrentPosition[0] = l / crangeh * hrange / hrange;
            op.dragEnd.call(this, "V", op.CurrentPosition[0]);
          }
        },
        /**
         * 事件初始化
         */
        _initEvent() {
          let self2 = this;
          let op = this.$Options;
          let hb = this.$HorizontalBar;
          let hs = this.$HorizontalSlider;
          let vb = this.$VerticalBar;
          let vs = this.$VerticalSlider;
          let el = this.$Element;
          window.addEventListener("resize", function(e) {
            self2.refresh();
          });
          this._sliderColorEvent(vs, op.VSbgColor, op.VSoverColor, op.VSclickColor);
          this._sliderColorEvent(hs, op.HSbgColor, op.HSoverColor, op.HSclickColor);
          el.addEventListener("mouseover", function(e) {
            hb.style.opacity = 1;
            vb.style.opacity = 1;
          });
          el.addEventListener("mouseleave", function(e) {
            if (op.AutoShow) {
              hb.style.opacity = 0;
              vb.style.opacity = 0;
            }
          });
          this._verticalSlide();
          this._horizontalSlide();
          function docMouseup() {
            let et = document.createEvent("MouseEvents");
            et.initEvent("mouseup", true, true);
            document.documentElement.dispatchEvent(et);
          }
          let iframe = document.querySelectorAll("iframe");
          for (let i = 0; i < iframe.length; i++) {
            iframe[i].addEventListener("mouseover", docMouseup);
          }
          document.documentElement.addEventListener("mouseleave", docMouseup);
        }
      };
      return new VirtualScroll(RootElement, UserConfig);
    });
  }
});
export default require_virtualscroll();
//# sourceMappingURL=virtualscroll.js.map
