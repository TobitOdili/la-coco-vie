node_modules/
.nuxt/
.output/
.env
dist
.claude/settings.local.json
.DS_Store
# Local-only: screen-capture frames shared for exit-animation tuning (never commit — large)
millanova frames/
exit-frames/
*.frames/
